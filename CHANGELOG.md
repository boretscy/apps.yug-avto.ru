# Changelog - apps.yug-avto.ru (Монолит / Go API)

## [2026-07-29]
### Added
- Настроен беспарольный пуш и деплой на GitHub по SSH напрямую с сервера `yapps` (`46.29.9.98`).
- В `.gitignore` добавлены правила исключения плагинов, загрузок и временных файлов.

### Changed
- Выполнен перенос дев-окружения Go API со старого домена `apps.avatr-yugavto.ru` на новый `atest.yug-avto.ru`.
- Обновлена конфигурация окружения Go API дева в `go/.env` и `go/internal/cis/meta.go` с указанием нового домена.
- Скомпилирована новая версия Go API дева и перезапущен фоновый демон на порту `8081`.
